[package]
name = "frigate"
version = "0.0.1"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
image = { version = "0.25.10", default-features = false, features = ["jpeg", "png"] }
ab_glyph = "0.2.32"
