# Run with `dart run ffigen --config ffigen.yaml`.
name: FrigateDrawBindings
description: |
  Bindings for `src/frigate_draw.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/frigate_draw_bindings_generated.dart'
headers:
  entry-points:
    - 'src/frigate_draw.h'
  include-directives:
    - 'src/frigate_draw.h'
ffi-native:
comments:
  style: any
  length: full
