name: frigate_draw
description: "A new Dart FFI package project."
version: 0.0.1

environment:
  sdk: ^3.11.4

dependencies:
  code_assets: ^1.0.0
  hooks: ^1.0.2
  native_toolchain_rust: ^1.0.3

dev_dependencies:
  ffi: ^2.2.0
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
