[package]
name = "frigate_draw"
version = "0.0.1"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
image = { version = "0.25.10", default-features = false, features = ["jpeg", "png"] }
tiny-skia = "0.12.0"
